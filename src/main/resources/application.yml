server:
  port: 8080

spring:
  application:
    name: student-management

  datasource:
    url: jdbc:mysql://localhost:3306/student_management?useSSL=false&serverTimezone=UTC
    username: root
    password: Luc123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto=validate:

  sql:
    init:
      mode: never

app:
  jwt:
    secret: 9a7f2c8e4b3d2a1f9a7f2c8e4b3d2a1f9a7f2c8e4b3d2a1f
    expiration: 86400000

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
    display-request-duration: true
    try-it-out-enabled: true
